#include "main.h"
/**
* _abs : prints the absolute value of a number
*@i: number to be verified
*Return: returns 0 or i 
*/
int _abs(int i)
{
if (i > 0)
return (i);
else if (i < 0)
return (-i);
else
return (0);
}
